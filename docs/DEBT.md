# 技术债务清单

> 每轮清理的入口:从这里挑选条目,处理后更新状态与日期。
> 出处:GOALS/README 的诚实记录、代码审查、文档-实现核对。

## 正确性 / 文档-实现不一致

| # | 条目 | 状态 |
|---|------|------|
| C1 | VM 运行时错误无位置(指令不带行列) | ✅ 2026-08-15 行号表(`Function.lines`,错误带行号) |
| C2 | mojibake 注释(`闂?` UTF-8 截断)8 处 | ✅ 2026-08-15 清除 |
| C3 | `-x` 用 `0 - x` hack 编译(多余指令) | ✅ 2026-08-15 专用 `Instr::Neg` |
| C4 | List/Tuple 索引越界静默返回 Unit | ✅ 2026-08-15 运行时错误 E0223(带行号) |
| C5 | Json(运行时原生值)与 `None` 比较报 CmpMismatch | ✅ 2026-08-15 支持(文档 D13 声称) |
| C6 | 静态错误码 E03xx(IndexNotInt/IndexOnNonList)被 VM 用作运行时错误 | ☐ 应归 E02xx 求值段 |
| C7 | `for` 仅支持内建容器,自定义类型迭代接口未实现(D6 声称"翻译为迭代接口调用") | ☐ 文档已降级;接口迭代留待设计 |
| C8 | 求值错误只有行号没有列(README 已如实改为"行号") | ☐ 如需列:行表存 (line, col),当前收益低 |

## 设计妥协(GOALS 诚实记录)

| # | 条目 | 状态 |
|---|------|------|
| D1 | `int` 为 `i64`,设计目标是任意精度(§4.1) | ☐ 大工程:需 bignum 内建 |
| D2 | Dict/Set 运行时为线性结构(无哈希),O(n) 查找(§8) | ☐ 哈希化,注意 `Json` 动态 key |
| D3 | 借用检查为最小规则集:无索引借用/细粒度区域(§6.5) | ☐ 设计先行 |
| D4 | `select` 多路复用未实现(§7.5) | ☐ 等真实需求 |
| D5 | `Shared[T]`/`Mutex` 未实现(§6/§7.4) | ☐ 标准库后置 |
| D6 | LSP hover 与增量同步未实现(D14) | ☐ |
| D7 | formatter 丢弃注释(词法器不保留)(D14) | ☐ |
| D8 | 模块系统为编译期符号合并,无运行时隔离(D13) | ☐ 设计决策待定 |
| D9 | CLI 不支持向脚本传参(演示用 test 驱动 main) | ☐ M5 |
| D10 | `x = f(x)` 自赋值被借用检查拒绝(skip 不穿透调用实参) | ☐ M5;绕法:临时变量/mut ref |

## 性能

| # | 条目 | 状态 |
|---|------|------|
| P1 | 指令分派 ~4-6ns/条(理论下限 ~2ns)(bench/README) | ☐ 需先建基准回归 |
| P2 | `Value` 24B 拷贝开销 | ☐ 与 P1 同轮处理 |
| P3 | Json 每节点 `to_str()` 字符串化再判类型(json_tool 模式) | ☐ 可加 `is_container` 类快速路径 |
| P4 | 调用/方法分发在热路径多次查表(methods/strings) | ☐ 与 P1 同轮 |

## 结构

| # | 条目 | 状态 |
|---|------|------|
| S1 | typecheck.rs ~3500 行、vm.rs ~2200 行单文件 | ☐ 按模块拆分(类型检查/借用/表达式) |
| S2 | 错误码渲染逻辑分散(vm 里 err 用静态码) | ☐ 与 C6 同轮 |

## 维护记录

- 2026-08-15 建立清单(第一轮"正确性优先"清理: C1-C5 完成,C6-C8 记录;
  同时修正文档-实现不一致: D6 降级、README 错误定位表述)
